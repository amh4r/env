return {
  "akinsho/bufferline.nvim",
  opts = {
    options = {
      -- Show ordinal numbers on tabs so <leader>b1, <leader>b2, etc. match
      -- visual position
      numbers = "ordinal",
    },
  },
}
